/* Name: Find Nearest square number
 * Level: 8kyu
 * Instructions: Your task is to find the nearest square number,
 * nearest_sq(n), of a positive integer n.
 * 
 * Goodluck :)
 */

const nearestSq = num => Math.round(num ** 0.5) ** 2;
