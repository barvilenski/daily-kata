/* Name: Bin to Decimal
 * Level: 8kyu
 * Instructions: Complete the function which converts a binary number
 * (given as a string) to a decimal number.
*/

const binToDec = binary => parseInt(binary, 2);
